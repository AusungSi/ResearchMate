export default function PaperWorkbenchChatSidebarCleanUpload() {
  const messages = [
    {
      role: "user",
      text: "这篇论文和 R2GenGPT 的核心差异是什么？重点看生成过程是否更可控。",
      time: "12:32",
    },
    {
      role: "assistant",
      text:
        "核心差异在于生成路径是否先经过显式 planning。当前论文先在受约束的子图空间里生成 plan，再写报告，因此更容易在节点级别约束内容范围；R2GenGPT 更偏直接利用大模型完成生成，控制粒度通常更粗。",
      bullets: [
        "当前论文：plan-first，可在节点/关系层加约束",
        "R2GenGPT：generation-first，表达更自由但可控性较弱",
        "建议下一步比较训练监督方式、推理时约束机制和误差传播路径",
      ],
      time: "12:33",
    },
  ];

  const refs = [
    "SGP-Gen.pdf · Method section",
    "R2GenGPT · Related work",
    "Project notes · Planning comparison",
  ];

  return (
    <div className="min-h-screen bg-slate-100 p-8 text-slate-900">
      <div className="mx-auto flex max-w-[420px] justify-center">
        <div className="h-[860px] w-[390px] overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-[0_24px_80px_rgba(15,23,42,0.14)]">
          <div className="flex h-full flex-col bg-[linear-gradient(to_bottom,rgba(248,250,252,0.96),rgba(255,255,255,1))]">
            <header className="border-b border-slate-200 bg-white/90 px-4 py-3 backdrop-blur">
              <div className="flex items-start justify-between gap-3">
                <div className="min-w-0">
                  <div className="flex items-center gap-2">
                    <span className="inline-flex h-8 w-8 items-center justify-center rounded-xl bg-slate-900 text-sm font-semibold text-white">
                      AI
                    </span>
                    <div>
                      <div className="text-sm font-semibold">Research Chat</div>
                      <div className="text-xs text-slate-500">Clean sidebar mode</div>
                    </div>
                  </div>
                </div>
                <div className="flex items-center gap-2">
                  <button className="rounded-lg border border-slate-200 bg-white px-2.5 py-1.5 text-xs text-slate-600">New</button>
                  <button className="rounded-lg border border-slate-200 bg-white px-2.5 py-1.5 text-xs text-slate-600">⋯</button>
                </div>
              </div>

              <div className="mt-3 flex flex-wrap gap-2">
                <span className="inline-flex items-center rounded-full border border-blue-200 bg-blue-50 px-2.5 py-1 text-[11px] font-medium text-blue-700">
                  GPT-5.4
                </span>
                <span className="inline-flex items-center rounded-full border border-slate-200 bg-slate-100 px-2.5 py-1 text-[11px] font-medium text-slate-600">
                  Workspace theme synced
                </span>
              </div>
            </header>

            <main className="min-h-0 flex-1 overflow-y-auto px-4 py-4">
              <div className="space-y-4">
                <div className="rounded-2xl border border-dashed border-slate-300 bg-slate-50 px-4 py-3 text-xs leading-5 text-slate-500">
                  通过底部按钮附加论文、笔记、PDF 段落或比较对象后，再围绕内容继续对话。
                </div>

                {messages.map((msg, idx) => (
                  <div key={idx} className={`flex ${msg.role === "user" ? "justify-end" : "justify-start"}`}>
                    <div className={`max-w-[92%] ${msg.role === "user" ? "items-end" : "items-start"} flex flex-col`}>
                      <div
                        className={
                          msg.role === "user"
                            ? "rounded-2xl rounded-br-md bg-slate-900 px-4 py-3 text-sm leading-6 text-white shadow-sm"
                            : "rounded-2xl rounded-bl-md border border-slate-200 bg-white px-4 py-3 text-sm leading-6 text-slate-700 shadow-sm"
                        }
                      >
                        <p>{msg.text}</p>
                        {msg.bullets && (
                          <ul className="mt-3 space-y-2 border-t border-slate-200 pt-3 text-sm text-slate-600">
                            {msg.bullets.map((b) => (
                              <li key={b} className="flex gap-2">
                                <span className="mt-[8px] h-1.5 w-1.5 rounded-full bg-blue-500" />
                                <span>{b}</span>
                              </li>
                            ))}
                          </ul>
                        )}
                      </div>
                      <div className="mt-1.5 px-1 text-[11px] text-slate-400">{msg.time}</div>
                    </div>
                  </div>
                ))}

                <div className="rounded-2xl border border-blue-100 bg-blue-50/70 p-3">
                  <div className="text-[11px] font-semibold uppercase tracking-[0.16em] text-blue-500">Attached references</div>
                  <div className="mt-2 space-y-2">
                    {refs.map((ref) => (
                      <div key={ref} className="flex items-center justify-between rounded-xl border border-blue-100 bg-white px-3 py-2">
                        <div className="truncate text-xs text-slate-600">{ref}</div>
                        <button className="ml-3 rounded-lg border border-slate-200 px-2 py-1 text-[11px] text-slate-500">Open</button>
                      </div>
                    ))}
                  </div>
                </div>
              </div>
            </main>

            <footer className="border-t border-slate-200 bg-white p-4">
              <div className="mb-3 flex flex-wrap items-center gap-2">
                <button className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-medium text-slate-600">+ Upload paper</button>
                <button className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-medium text-slate-600">+ Add note</button>
                <button className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-medium text-slate-600">+ Compare paper</button>
                <button className="rounded-xl border border-slate-200 bg-slate-50 px-3 py-2 text-xs font-medium text-slate-600">+ Quote PDF</button>
              </div>

              <div className="rounded-3xl border border-slate-200 bg-slate-50 p-2 shadow-inner">
                <div className="min-h-[92px] rounded-2xl bg-white px-3 py-3 text-sm leading-6 text-slate-400">
                  Ask anything about the attached material, compare methods, extract evidence, or continue the discussion...
                </div>
                <div className="mt-2 flex items-center justify-between px-1">
                  <div className="flex items-center gap-2 text-slate-400">
                    <button className="rounded-lg p-2 hover:bg-slate-100">📎</button>
                    <button className="rounded-lg p-2 hover:bg-slate-100">#</button>
                    <button className="rounded-lg p-2 hover:bg-slate-100">⌘</button>
                  </div>
                  <button className="rounded-2xl bg-slate-900 px-4 py-2 text-sm font-medium text-white shadow-sm">
                    Send
                  </button>
                </div>
              </div>
            </footer>
          </div>
        </div>
      </div>
    </div>
  );
}
